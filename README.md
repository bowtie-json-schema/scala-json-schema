# scala-json-schema

A [Bowtie](https://github.com/bowtie-json-schema/bowtie) test harness.

Its image is published to `ghcr.io/bowtie-json-schema/scala-json-schema` and run via `bowtie run -i scala-json-schema`.
